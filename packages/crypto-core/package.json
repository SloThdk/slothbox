{
  "name": "@slothbox/crypto-core",
  "version": "0.2.14",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./symmetric": "./src/symmetric.ts",
    "./derivation": "./src/derivation.ts",
    "./utils": "./src/utils.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'crypto-core uses tsc strict mode for static checks; no eslint config' && exit 0",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "libsodium-wrappers-sumo": "^0.7.15"
  },
  "devDependencies": {
    "@types/libsodium-wrappers": "^0.7.13",
    "@types/node": "^25.9.1",
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  }
}
