apiVersion: 1

providers:
  - name: "SlothBox dashboards"
    orgId: 1
    folder: "SlothBox"
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /etc/grafana/provisioning/dashboards
